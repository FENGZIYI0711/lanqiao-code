#include <iostream>
#include <cstring>
#include <queue>
using namespace std;
int main(){
    int n,m,flag;
    string people;
    cin>>n;
    queue<string> q;
    for (int i=0;i<n;i++){
        cin>>people;
        q.push(people);
    }
    cin>>m;
    while(m--){
        cin>>flag;
        if(flag==1&&q.empty()!=1){
            q.pop();
        }else if(flag==2){
            cin>>people;
            q.push(people);   
        }
    }
    if(q.empty()){
        cout<<"There are no more people in the queue."<<endl;
    }else{
        cout<<q.front()<<endl;
    }
}
